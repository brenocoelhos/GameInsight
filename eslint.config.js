import vuetify from 'eslint-config-vuetify'

export default vuetify({
  rules: {
    'sort-imports': 'off',
    'import/order': 'off',
    'sort-keys': 'off',
    'vue/sort-keys': 'off',
    'vue/attributes-order': 'off',
  },
})
